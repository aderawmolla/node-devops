<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match **>
  @type opensearch
  host opensearch 
  port 9200
  scheme http
  logstash_format true
  include_tag_key true
  tag_key @log_name
  index_name fluentd
  flush_interval 5s
</match>
