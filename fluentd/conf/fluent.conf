<source>
  @type forward
  port 24224
</source>

<match **>
  @type opensearch
  host opensearch
  port 9200
  logstash_format true
  include_tag_key true
  tag_key @log_name
  <buffer>
    @type memory
    flush_interval 5s
  </buffer>
</match>
